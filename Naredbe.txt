Baze naredbe
CREATE KEYSPACE rbus;
funkcija uuid() generise jedinstveni id
id uuid tip
TRUNCATE -removes all data
CREATE TABLE "Autoprevoznik"(
    "AutoprevoznikID" uuid,
	username text,
    password text,
    email text,
	naziv text,
	telefon text,
	PRIMARY KEY (email,"AutoprevoznikID")
);
INSERT INTO rbus."Autoprevoznik" ("AutoprevoznikID",username,password,email,naziv,telefon)
VALUES(uuid(),'jugo65','abc123','stefan@hotmail.com','Jugoprevoz','061234567');

CREATE TABLE "Linija"(
  "start" text,
  "cilj" text,
  "LinijaID" uuid,
  PRIMARY KEY ("LinijaID")
);
CREATE TABLE "TerminType"(
"TerminTypeId" uuid,
"AutoprevoznikID" uuid,
"LinijaID" uuid,
 dan text,
 vreme text,
 cena int,
 mesta int,
 PRIMARY KEY ("TerminTypeId","LinijaID")
);
CREATE TABLE "Termin"(
  "TerminID" uuid,
  "TerminTypeID" uuid,
  mesta int,
  popunjena int,
  datum text,
  PRIMARY KEY ("TerminTypeID", datum,"TerminID")
);
CREATE TABLE "Sedista"(
  "TerminID" uuid,
  "brojsedista" int,
  PRIMARY KEY ("TerminID","brojsedista")
);
CREATE TABLE "Rezervacija" (
    "resID" text,
	"TerminID" uuid,
	vremerez date,
    "userID" uuid,
    PRIMARY KEY ("resID","TerminID","userID")
);
CREATE TABLE "Korisnik"(
   id   uuid,
   username text,
   password text,
   name text,
   phone text,
   email text,
   PRIMARY KEY(email,id)
);